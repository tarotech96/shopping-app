import 'package:flutter/material.dart';

class CartEmplty extends StatelessWidget {
  const CartEmplty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
